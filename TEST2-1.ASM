DATA 	SEGMENT 
X               DB      30H
Y               DB      50H
Z               DB      34H
MAX 		DB	?
DATA	ENDS
CODE	SEGMENT
	ASSUME	CS:CODE,	DS:DATA
START:	
		MOV AX,DATA
		MOV	DS,AX

		MOV	AL,X
		CMP	AL,Y
		JB	CHANGE1
		JMP	NEXT1

CHANGE1:	MOV	AL,Y
NEXT1:	MOV	MAX,AL
		CMP	AL,Z
		JB	CHANGE2
		JMP	NEXT2
CHANGE2:MOV	AL,Z
NEXT2:	MOV	MAX,AL


		MOV	AH,4CH
		INT	21H
CODE	ENDS
	END START
